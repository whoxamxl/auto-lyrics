name: Apply final Codex fixes

on:
  push:
    branches:
      - fix/resolve-past-codex-findings

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/resolve-past-codex-findings
      - name: Apply reviewed source fixes
        run: python3 .github/scripts/apply_source_regression_fixes.py
      - name: Commit source fixes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src
          git commit -m "Address final Codex source regressions"
          git push origin HEAD:fix/resolve-past-codex-findings
